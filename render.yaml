services:
  - type: web
    name: mutemi-mpesa-analyzer
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run mutemi_mpesa_mobile_app.py --server.port $PORT --server.address 0.0.0.0
